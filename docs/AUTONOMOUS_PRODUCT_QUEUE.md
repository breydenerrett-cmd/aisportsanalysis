# Autonomous product queue

Source of truth for product-mode work (owner directive 2026-09-06 ~23:00Z:
"from research platform to polished demo / beta"). Supersedes
`docs/WEEKEND_QUEUE.md` for dispatch; that file stays as the weekend record.

Statuses: READY / RUNNING / VERIFYING / DONE / BLOCKED_HUMAN /
BLOCKED_EXTERNAL / FAILED_RETRYABLE / FAILED_TERMINAL / DEFERRED.
Model: Haiku = recon/mechanical, Sonnet = implementation, Opus = methodology
or adversarial review only, Fable = orchestration. One primary stream plus at
most one bounded review stream at a time.

| ID | Pri | Objective | Deps | Model | Status | Acceptance criteria | Evidence | Commit | Blocker |
|---|---|---|---|---|---|---|---|---|---|
| P0-1 | P0 | Forward capture runs from GitHub Actions without the Parent session: register workflow on the default branch, one dispatched run, then one SCHEDULED run with no interactive help; record run IDs, deltas, credit accounting, lock/overlap behaviour; downgrade in-session capture to fallback-only | – | Fable (bash) | VERIFYING | (a) dispatched run green with a bot commit on the working line; (b) a `schedule`-event run green with its own commit while no interactive command ran; (c) a paid dense capture (games in window) lands from Actions with the credit log showing `live_capture`; (d) in-session trigger only launches when the external heartbeat is stale | default-branch commit 25816b8; run 34066005061 (dispatch) SUCCESS 23:07:55–23:08:36Z, capture step 30s, commit 16a2b97 by forward-capture-bot, 38 rows (watch/weather/info events), 0 odds credits (no game in window), balance 24,889 before and after, no ESCALATE, secret consumed as `***` only; (b) first genuine schedule run 34071611931 SUCCESS 01:00:46Z → bot commit 0bc53ca, no ESCALATE; cloud capture routine switched to FALLBACK-ONLY at 01:04Z; one duplicate window (01:00) from the pre-cutover fallback recorded in docs/OVERNIGHT_RUN.md; still to observe: a second consecutive scheduled run and explicit concurrency-group overlap evidence | 25816b8, 16a2b97, 0bc53ca | – |
| P0-2 | P0 | Move the 10:00Z daily loop (results ingest, brief, slate, settle, eod, box lines) to an Actions job so decisions/settlement do not depend on the Parent session; two writers on the same stores must not race | P0-1 | Sonnet | VERIFYING | one scheduled daily run green end to end from a fresh checkout; git-ignored inputs rebuilt by the job; no rebase conflict with capture slots (shared concurrency group or ordering); ESCALATE lines surfaced in the run log | Sonnet worker landed 73d37dc: scripts/daily_bootstrap.sh (refuses on missing Statcast manifest, rebuilds results/pitcher/bullpen from free endpoints), .github/workflows/daily-loop.yml (cron 0 10 * * *, shared `forward-capture` concurrency group, actions/cache, ESCALATE fails job, only secret ODDS_API_KEY), 48 tests green. Open: Statcast store seeding on a cold runner (P0-2b worker: orphan seed branch + bootstrap fallback); workflow not yet on the default branch; no Actions run yet; 98a31ed seed fallback; default-branch 2b63f0d; dispatched run 34068303330: bootstrap OK on cold runner, bot commit 560f900, honest ESCALATE (settle refused, games in progress at 00:04Z) — failure path proven; scheduled 10:00Z run not yet observed | 73d37dc, 98a31ed, 560f900 | – |
| P0-3 | P0 | Reliability baseline: one `ops status` command whose green means evidence of recent OUTPUT per stage (capture, feature refresh, gamekey map, pitch store, slate, settle, eod, staging, credit band, failed jobs), plus a regression test per incident class seen this week (stale L1, stale pitch store, unmapped game_pk, mid-sleep death) | – | Sonnet | READY | command exits non-zero with named stage when any stage lacks recent output; tests pin each incident; documented in docs/OBSERVABILITY.md | – | – | – |
| P0-4 | P0 | Write docs/BETA_ACCEPTANCE_CRITERIA.md from the owner's 13-point definition of done, each point mapped to a concrete surface, data source, and test/check; MODEL LIKES SIDE vs PRICE OFFERS VALUE distinction explicit | – | Fable + Haiku recon | DONE | every point has an owner surface and a verifiable check; gaps listed as queue tasks | docs/BETA_ACCEPTANCE_CRITERIA.md: 13 points mapped; 2 DONE, 7 PARTIAL, 4 MISSING at 16a2b97 | e20f751 | – |
| P1-1 | P1 | Beta-gap assessment of the customer surface (web/index.html, web/js/today.js, odds.js, betcheck.js, api/app.py routes): what exists vs the slate/Bet Check/performance/explainability requirements; output a ranked gap list, no code | P0-4 | Haiku recon → Fable | DONE | docs/BETA_GAP_ASSESSMENT.md with per-requirement status (present / partial / missing) and the API/data path behind each | 31 routes, 8 screens, 4 matrices; biggest gap: engine decisions/paper performance never reach the product API | e20f751 | – |
| P1-2 | P1 | Today's slate experience: every analyzed opportunity shows matchup, market, book/consensus, price, implied + de-vig prob, model prob, edge, EV, evidence tier, depth, last-updated, reasons, warnings, status; filter/rank controls | P1-1 | Sonnet | READY | staging renders the slate with live data; freshness timestamps truthful; empty/no-play state honest | – | – | – |
| P1-3 | P1 | Bet Check verdict language (STRONG VALUE … INSUFFICIENT DATA) with fair price, implied vs model prob, factors, risks, book disagreement; verdict rules documented and tested | P1-1 | Sonnet | READY | unit tests over verdict boundaries; no verdict without a defensible fair price; INSUFFICIENT DATA when inputs stale/missing | – | – | – |
| P1-4 | P1 | Signal registry: machine-readable SUPPORTED/WEAK/NULL/REJECTED/EXPLORATORY/FORWARD-ONLY for every family/signal (F5, totals, V1–V4, M1–M5, evolab families) and product copy that never presents NULL/REJECTED as a recommendation | – | Sonnet | READY | registry file + test that product reason strings reference only SUPPORTED/FORWARD-ONLY signals as evidence | – | – | – |
| P1-5 | P1 | Confidence/evidence tier system (sample support, feature completeness, calibration, depth, agreement, price sensitivity, staleness, lineup confirmation) separate from model probability | P1-4 | Sonnet (+Opus review of the tiering rule) | READY | documented rule; tests; UI shows tier distinct from probability | – | – | – |
| P1-6 | P1 | Performance area: paper picks, W/L/push, ROI, odds, CLV, calibration, buckets, family, drawdown, BET WON vs REASONING CORRECT split | – | Sonnet | READY | reads evidence/paper_accounts + reviews; numbers reconcile with settle/eod outputs | – | – | – |
| P1-7 | P1 | Demo mode: frozen historical slate replay, clearly labelled DEMO / HISTORICAL REPLAY, never styled as live | P1-2 | Sonnet | READY | toggle/route; banner on every demo surface; timestamps show the replayed date | – | – | – |
| P1-8 | P1 | Staging verification script after each deploy: health, primary routes, data freshness, JS errors, no secrets in responses | – | Haiku | READY | script + wired into deploy-staging.yml as a post-deploy check | – | – | – |
| P2-1 | P2 | Onboarding copy (probability, edge, price, confidence, PASS, why nothing today) | P1-2 | Haiku | DEFERRED | – | – | – | – |
| P2-2 | P2 | Feedback capture (useful / confusing / bad price / stale / missing market / UI) stored append-only, not fed to models | P1-2 | Sonnet | DEFERRED | – | – | – | – |

Carried over from the weekend queue: W4 (T−2h comparator purchase, ~4–5k credits) stays BLOCKED_HUMAN (paid historical acquisition beyond the approved envelope).
