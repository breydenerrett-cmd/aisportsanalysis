"""Capture governance: credit budget and cadence SLO for forward collection.

Split out from the individual capture modules (dense.py, prop_listing.py,
prop_prices.py) so the spend rules live in one place instead of being
re-derived per module. See src/capture/budget.py and src/capture/cadence.py.
"""
